{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Raylib Başlat",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Raylib Build",
            "terminal": "external"
        }
    ]
}